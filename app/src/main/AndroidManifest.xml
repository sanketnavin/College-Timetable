<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spit.timetable.timetablespit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BaseActivity"
            android:label="@string/title_activity_basic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddContentActivity"
            android:label="Add Contents"></activity>
        <activity
            android:name=".AddLectureActivity"
            android:label="Add Lecture"></activity>
        <activity
            android:name=".ModifyLectureActivity"
            android:label="Modify Lecture"></activity>
    </application>

</manifest>