<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BaseActivity"
    android:background="#fff" >

    <com.alamkanak.weekview.WeekView
        android:id="@+id/weekView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:firstDayOfWeek="monday"
        app:showFirstDayOfWeekFirst="true"
        app:eventTextColor="@android:color/white"
        app:eventPadding="4dp"
        app:textSize="12sp"
        app:hourHeight="55dp"
        app:minHourHeight="50dp"
        app:headerColumnPadding="8dp"
        app:headerColumnTextColor="@color/toolbar_text"
        app:headerRowPadding="12dp"
        app:columnGap="1dp"
        app:noOfVisibleDays="6"
        app:xScrollingSpeed="0.0"
        app:showNowLine="true"
        app:pastBackgroundColor="@color/past_color"
        app:futureBackgroundColor="@android:color/white"
        app:showDistinctPastFutureColor="true"
        app:headerRowBackgroundColor="@color/toolbar"
        app:dayBackgroundColor="@color/day_color"
        app:todayBackgroundColor="@color/today_color"
        app:headerColumnBackground="#ffffffff"
        app:todayHeaderTextColor="@color/accent" />


    <ProgressBar
        android:id="@+id/loading_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/Widget.AppCompat.ProgressBar"
        android:layout_centerInParent="true" />


</RelativeLayout>
